# Copy this file to ".env" and adjust if needed.

# The URL where your backend server is running.
# For local development, leave this as-is (matches "npm start" in the backend folder).
# Once you deploy the backend (e.g. to Render), replace this with that live URL.
VITE_API_URL=http://localhost:5000
